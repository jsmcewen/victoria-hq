# Copy to .env next to docker-compose.yml (never commit the real .env).
# BETTER_AUTH_URL must be the https address the iPads actually open.

BETTER_AUTH_URL=https://chores.example.com
BETTER_AUTH_SECRET=
POSTGRES_PASSWORD=victoria-change-me
XAI_API_KEY=
